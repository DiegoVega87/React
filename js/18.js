// Condicionales

const available = 4000;
const withdraw = 200;

const auth = true;

if (available > withdraw) {
  // Meets the condition
  console.log("Can withdraw");
} else {
  // Condition not met
  console.log("Not enough funds");
}

/**
 * > greater than
 * < less than
 * >= greater or egual
 * <= less than or equal
 * == equal
 * === Strictly equal
 * !== Not equal
 */
